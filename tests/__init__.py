import os
import sys

# Add the app directory to the sys.path so that the app module can be imported easily
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

# You can also perform other common setup tasks here if needed
